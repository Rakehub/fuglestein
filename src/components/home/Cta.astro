---
interface Props {
  title: string;
  description: string;
  buttonText: string;
  href: string;
}

const { title, description, buttonText, href } = Astro.props;
---

<section class="py-16 reveal sm:py-20 bg-neutral-50/50 dark:bg-neutral-950/20">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">

    <div class="relative mx-auto max-w-6xl overflow-hidden rounded-3xl bg-neutral-950 px-6 py-16 text-center shadow-xl sm:px-12 sm:py-20 border border-primary-500/10 dark:border-primary-400/10">

      <div class="absolute inset-0 h-full w-full pointer-events-none">
        <video
          src="/assets/video_final.mp4"
          class="h-full reveal w-full object-cover opacity-75"
          autoplay
          muted
          loop
          playsinline
        >
        </video>
        <div class="absolute inset-0 bg-neutral-950/40"></div>
        <div class="absolute inset-0 bg-gradient-to-t from-neutral-950/80 via-neutral-950/20 to-transparent sm:hidden"></div>
      </div>

      <div class="relative mx-auto max-w-2xl flex flex-col items-center gap-6 p-0 sm:bg-black/40 sm:backdrop-blur-md sm:p-8 sm:rounded-3xl">
        <h2
          class="text-3xl font-light italic tracking-tight text-white sm:text-4xl"
          style="font-family: 'Cormorant Garamond', Georgia, serif;"
        >
          {title}
        </h2>
        <p
          class="text-lg leading-relaxed text-neutral-100"
          style="font-family: 'Cormorant Garamond', Georgia, serif;"
        >
          {description}
        </p>
        <div class="pt-2">
          <a href={href} style="background-color: #ffffff !important; color: #0a0a0a !important;" class="inline-flex items-center justify-center rounded-full px-6 py-3 text-sm font-semibold shadow-sm transition-all duration-300 hover:bg-amber-400 hover:shadow-md active:scale-95">
            {buttonText}
          </a>
        </div>
      </div>

    </div>
  </div>
</section>